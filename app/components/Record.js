const Record = ({left, right}) => (
    <>
    <span className='font-medium text-gray-500'>{left}</span>
    <span className='border-l px-4 text-gray-500'>{right}</span>
    </>
);

export default Record;